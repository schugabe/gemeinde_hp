<div class="row">
<ul class="nav nav-tabs span10">
	<%= devise_tab link_to(t("user.signin"), new_session_path(resource_name)), 'sessions'%>
	<%= devise_tab link_to(t("user.signup"), new_registration_path(resource_name)), 'registrations'%>
	<%= devise_tab link_to(t("user.forgot"), new_password_path(resource_name)), 'passwords'%>
	<%= devise_tab link_to(t("user.confirmation"), new_confirmation_path(resource_name)), 'confirmations'%>
</ul>
</div>